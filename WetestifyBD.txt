\c wetestify;


SELECT * FROM personnel;

SELECT COUNT(id) as "Nombre d'id"
FROM personnel;

-- Ajout du champ statut --
ALTER TABLE personnel ADD COLUMN statut VARCHAR(20);

-- Insertion des valeurs dans e champ statut
UPDATE personnel SET statut = 'Administrateur' WHERE id = 1;
UPDATE personnel SET statut = 'Administrateur' WHERE id = 2;

/*
DELETE FROM personnel
WHERE id = 6 ;

SELECT pg_get_serial_sequence('Personnel', 'id');

SELECT last_value FROM public.personnel_id_seq;

ALTER SEQUENCE public.personnel_id_seq RESTART WITH 3;

SELECT setval('public.personnel_id_seq', 2, true);

INSERT INTO Personnel (nom, ...) VALUES ('Test', ...);
*/

